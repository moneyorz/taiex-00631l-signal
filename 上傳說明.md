# GitHub 手動上傳說明

這個 `upload` 資料夾內放的是上傳到 GitHub 的最小必要檔案。請上傳「資料夾內的內容」，不要把外層 `upload` 資料夾本身包進 repository。

## 需要上傳的檔案結構

上傳完成後，GitHub repository 根目錄應該長這樣：

```text
index.html
data/
  taiex.csv
  taiex_metadata.json
scripts/
  update_taiex.py
.github/
  workflows/
    pages.yml
    update-taiex.yml
```

## 各檔案用途

- `index.html`：網頁主程式，顯示方案2訊號檢查器。
- `data/taiex.csv`：加權指數歷史資料。
- `data/taiex_metadata.json`：歷史資料目前最新日期、筆數與更新狀態。
- `scripts/update_taiex.py`：GitHub Action 用來增量更新加權指數歷史資料。
- `.github/workflows/update-taiex.yml`：定時與手動更新大盤歷史資料。
- `.github/workflows/pages.yml`：部署 GitHub Pages，讓網頁可以被公開瀏覽。

## 第一次手動上傳到 GitHub

1. 打開你的 GitHub repository。
2. 進入 `Code` 頁面。
3. 如果 repository 是空的，點 `uploading an existing file`。
4. 如果 repository 不是空的，點 `Add file`，再選 `Upload files`。
5. 打開本機的 `upload` 資料夾。
6. 將 `upload` 裡面的這些項目拖到 GitHub 上傳區：
   - `index.html`
   - `data`
   - `scripts`
   - `.github`
   - `上傳說明.md` 可傳可不傳
7. 確認 GitHub 上的路徑不是 `upload/index.html`，而是根目錄直接看到 `index.html`。
8. Commit message 可填：

```text
Add TAIEX signal checker
```

9. 點 `Commit changes`。

## 啟用 GitHub Pages

1. 到 repository 的 `Settings`。
2. 左側選 `Pages`。
3. `Build and deployment` 的 `Source` 選 `GitHub Actions`。
4. 回到 `Actions` 頁面。
5. 等 `Deploy GitHub Pages` 執行完成。
6. 完成後 GitHub Pages 會顯示可瀏覽的網址。

## 啟用 Action 寫入權限

如果更新大盤資料時無法自動 commit，請檢查：

1. 到 repository 的 `Settings`。
2. 左側選 `Actions`，再選 `General`。
3. 找到 `Workflow permissions`。
4. 選 `Read and write permissions`。
5. 儲存設定。

## 歷史大盤資料如何更新

`update-taiex.yml` 會在台灣時間週一到週五自動嘗試更新：

```text
16:00
16:30
17:00
17:30
18:00
18:30
19:00
19:30
20:00
20:30
21:00
```

它只更新歷史資料，不包含盤中即時資料。

## 手動更新大盤歷史資料

1. 到 repository 的 `Actions`。
2. 左側選 `Update TAIEX historical data`。
3. 點右側的 `Run workflow`。
4. Branch 選 `main`。
5. 再點一次 `Run workflow`。
6. 等執行完成。

如果有新資料，它會自動更新：

```text
data/taiex.csv
data/taiex_metadata.json
```

更新後 `Deploy GitHub Pages` 會再被觸發，網頁會顯示新的歷史資料最新日期。

## 增量更新邏輯

更新程式不是每次重抓全部資料。

它會：

1. 讀取目前 `data/taiex.csv` 的最後日期。
2. 從最後日期往前 10 天開始抓，作為緩衝。
3. 補齊後面缺少的交易日資料。
4. 用日期合併，所以重複執行不會產生重複資料。
5. 如果最近幾日資料被來源修正，會用新資料覆蓋同日期舊資料。
6. 如果沒有新增或修正，檔案不會被改，也不會產生無意義 commit。

例如目前資料只到 `2026/5/15`，下一次正常執行會從約 `2026/5/5` 開始抓，補齊到來源能提供的最新歷史日期；原本已有的 `2026/5/5 ~ 2026/5/15` 不會重複新增。

## 網頁上的最新日期

網頁會讀取：

```text
data/taiex_metadata.json
```

並顯示目前歷史資料的最新日期。若 GitHub Action 已更新，但網頁日期還沒變，請等 Pages 部署完成後再重新整理網頁。
